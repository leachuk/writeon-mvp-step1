'use strict';

module.exports = Object.freeze({
  error : {
    NOT_PERMITTED : "Operation not permitted",
    NO_RESULTS : "No results available",
    BAD_JSON_INPUT : "Malformed JSON input"
  }
});
