apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-test-volumeclaim
  namespace: default
  labels:
    app: nfs-test-volume
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: standard
  resources:
    requests:
      storage: 8Gi
status: {}

